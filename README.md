# Anugamana — Web

The frontend for Anugamana. Type what's on your mind and get a relevant Bhagavad Gita verse with an AI-generated explanation.

## Setup

```bash
npm install
npm run dev
```

Make sure the backend is running at `http://localhost:8000` before using the app.
